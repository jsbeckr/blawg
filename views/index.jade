each post in posts
	.post
		h2
			a(href="/post/" + post._id) #{post.title}
		p posted by #{post.author} on #{post.date}
		p!= post.body
.admin
	if user
		p 
			a(href="/admin") admin
			| 
			a(href="/logout") logout
	else
		p
			a(href="/login") login